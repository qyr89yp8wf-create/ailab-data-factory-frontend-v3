# 纯前端原型完整性检查

检查日期：2026-09-02

## 自动检查

- `npm run build`：通过。
- 用户端 `/`：HTTP 200。
- 运营端 `/admin.html`：HTTP 200。
- 源码中无 `127.0.0.1:8765`、`127.0.0.1:8770`、`tools/start_demo` 或 Axios 后端依赖。

## 用户端交互

- 首页四步流程与左侧导航可用。
- 数据中心列表、独立上传页、新数据集/新版本切换、数据集描述编辑和详情抽屉可用。
- 上传数据必须选择数据类型和已发布模板；格式校验通过后才能提交，上传隐私门禁与正式质检状态相互独立。
- 数据集版本：12 位哈希版本 ID、数字数据量、分页、横向滚动、固定操作列和下载入口可用。
- 版本详情：基础信息/质检报告切换可用，展示模板、来源任务、来源数据集版本和单行版本生成关系。
- 下游任务：质检/增强/扩增准入、创建确认、输入自动带入、下游任务列表及任务详情可用。
- 文档图像、对话、时序三类任务均支持数据合成、数据质检、数据增强、定向扩增四种单页表单。
- 合成任务只保留模板和批量生成设置，不包含质检、增强或扩增步骤。
- 质检任务全量检查并在原数据集中创建一个等量质检标注版本；输入版本保持不变。
- 质检任务支持按比例抽检并实时计算检查数量；抽检输出版本仍保留全部样本，未抽中样本写入“未质检”标签，且不能直接发起增强或定向扩增。
- 增强与定向扩增仅接受具备有效正式质检报告和样本级标签的版本，处理与自动复检只提交一个正式版本。
- 三类优化任务均以只读表格展示模板规则，支持分类、检查对象、判断方式筛选，以及规则搜索、展开和复制。
- 增强支持 PASS/自定义阈值选样、预置与自定义方案，并按隐私类型选择脱敏方式。
- 定向扩增支持覆盖缺口选择和多组自定义扩增设置，每组可配置标签、数量、指令、模型和参数。
- 任务详情包含输入血缘、规则快照、配置快照、实际 API 调用量、运行结果和唯一输出版本 ID。
- 文档种子模板：新建页、上传入口、面包屑和顶部操作栏可用。
- 虚构文档模板：完成 4 步配置、用途确认、试运行、质检和发布。
- 对话模板：完成 4 步配置、Prompt 编译、试运行和发布。
- 时序模板：完成 3 步配置、试运行和发布。
- API Keys：新增配置、添加多个模型、保存和刷新持久化可用。
- 用户个人信息和退出确认沿用原型交互。

## 运营端交互

- 用户操作日志：筛选、详情 JSON 抽屉和复制按钮可用。
- 任务执行日志：筛选、详情 JSON 抽屉和复制按钮可用。
- 用户管理：搜索、编辑、额度配置和删除入口可用；月度 Tokens 默认值为 1,000,000。
- 运营端已按当前原型范围移除公共 API 配置和 API 用量统计菜单。
- 全局系统时间统一展示为 `YYYY-MM-DD HH:mm:ss`；旧缓存中的短日期时间、斜杠格式和 ISO 时间会自动转换。
- 管理员个人信息和退出确认沿用原型交互。

## Mock 持久化

任务、数据集、模板和 API Keys 保存到浏览器 `localStorage`。清除 `127.0.0.1:8767` 的站点数据可恢复初始样例。
